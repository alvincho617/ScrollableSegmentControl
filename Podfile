# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ScrollableSegmentControl' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
	pod 'SnapKit'
  # Pods for ScrollableSegmentControl

  target 'ScrollableSegmentControlTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ScrollableSegmentControlUITests' do
    # Pods for testing
  end

end
